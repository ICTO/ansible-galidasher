---
- name: "Galidasher playbook"
  hosts: galidasher
  user: $user
  sudo: yes
  sudo_user: root

  vars_files:
    - vars/matterhorn.yml
    - vars/galidasher.yml
    - vars/agents.yml

  tasks:
    - include: tasks/setup.yml